#include <stdio.h>

int main()
{
	int i, j, sum;

	i = 10, j = 20; // 변수 i,j에 각각 10, 20을 저장
	sum = i + j; // int 변수 sum에 i와 j의 값을 더해서 저장

	printf("%d + %d = %d\n", i, j, sum); // 정수형 서식 %d를 통해 정수 출력

	return 0;
}

